sudo: required
services:
  - docker
git:
  submodules: false
script:
  - git submodule update --init
  - docker build -t imagemagick -f Dockerfile.imagemagick
  - ./createbook.sh
  - ./build.sh
after_success:
  - ./deploy.sh
